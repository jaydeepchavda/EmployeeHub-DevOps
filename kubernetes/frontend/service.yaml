apiVersion: v1
kind: Service
metadata:
  name: rhwalks-ui-service
  namespace: rhwalks-dev
spec:

  type: NodePort

  selector:
    app: rhwalks-ui
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
